import React, { FC } from "react";

interface ButtonProps {
  // Add any props you might need for the button
}

const Button: FC<ButtonProps> = () => {
  return <div>This is the web marketing button</div>;
};

export default Button;
